# Bind to 127.0.0.1 for safer default. Use 0.0.0.0 only behind firewall/TLS proxy.
BIND_ADDRESS=127.0.0.1

SONARQUBE_PORT=9000
SONAR_POSTGRES_USER=sonar
SONAR_POSTGRES_PASSWORD=change-me-long-random-password
SONAR_POSTGRES_DB=sonarqube

NEXUS_PORT=8081

