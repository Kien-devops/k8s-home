# Trivy scan policy (used by local scans and CI references)
# Copy to .env and adjust severity/policy as needed.

TRIVY_SEVERITY=HIGH,CRITICAL
TRIVY_EXIT_CODE=1
TRIVY_IGNORE_UNFIXED=false
TRIVY_TIMEOUT=10m
