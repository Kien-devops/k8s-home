apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: security-config
  namespace: argocd
  labels:
    app.kubernetes.io/name: security-config
    app.kubernetes.io/part-of: platform
spec:
  project: platform
  source:
    repoURL: https://github.com/Kien-devops/k8s-home.git
    targetRevision: main
    path: deploy/platform/security
  destination:
    server: https://kubernetes.default.svc
    namespace: security
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
